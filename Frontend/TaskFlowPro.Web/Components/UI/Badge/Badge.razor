@namespace TaskFlowPro.Web.Components.UI
@inherits BadgeBase

<span class="@GetBadgeClasses()" @attributes="AdditionalAttributes">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <i class="@Icon @(ChildContent != null ? "mr-1" : "") @GetIconClasses()"></i>
    }
    @ChildContent
</span>


